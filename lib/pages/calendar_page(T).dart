import 'package:flutter/material.dart';

class CalendarPageTeacher extends StatelessWidget {
  const CalendarPageTeacher({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('선생님 페이지'),
      )
    );
  }
}
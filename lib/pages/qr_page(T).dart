import 'package:flutter/material.dart';

class QrPageTeacher extends StatelessWidget {
  const QrPageTeacher({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('QR Page(T)')
      ),
    );
  }
}